server:
  port: 8083

spring:
  application:
    name: ACCOUNT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/banking_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/